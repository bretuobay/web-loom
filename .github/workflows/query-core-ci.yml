# .github/workflows/query-core-ci.yml

name: CI - @web-loom/query-core
on:
  pull_request:
    branches:
      - main
    paths:
      - 'packages/query-core/**'

jobs:
  build_and_test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Rollup native binaries work reliably on Node 20 & 22, NOT 23
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      # Prevent npm optional-dep failure by fully cleaning install
      - name: Clean install
        run: |
          rm -rf node_modules
          rm -f package-lock.json
          npm i

      # Ensure Vite and build dependencies are properly resolved
      - name: Rebuild build dependencies
        run: npm rebuild --workspace=@web-loom/query-core

      - name: Run tests for @web-loom/query-core
        run: npm test --workspace=@web-loom/query-core
      - name: Build @web-loom/query-core
        run: npx turbo run build --filter=@web-loom/query-core

      - name: Upload build artifacts (Optional)
        uses: actions/upload-artifact@v4
        with:
          name: query-core-dist-build-artifacts
          path: packages/query-core/dist/
