# API Reference: LocaleStore

## LocaleStore

A simple class for managing the current locale and subscriptions.

- `setLocale(locale)` — Sets the current locale
- `getLocale()` — Gets the current locale
- `subscribe(callback)` — Subscribes to locale changes
