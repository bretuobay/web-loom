# TypeScript Configuration

- Use strict mode for type safety
- All public APIs are fully typed
- Example:

```typescript
import type { Locale, SupportedLocale, Translations, I18nConfig } from '@web-loom/i18n-core';
```
