# Framework-Agnostic Patterns

## EventEmitter

Use `I18nEventEmitter` for event-based locale changes.

## Observable

Use `ObservableI18n` for observable state.

## Singleton

Use `createI18nInstance` for simple singleton pattern.
