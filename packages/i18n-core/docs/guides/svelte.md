# Svelte Integration Guide

## Setup

Import and use the `createI18nStore` from `examples/svelte/i18nStore.ts`.

## Example

```typescript
import { createI18nStore } from '../../examples/svelte/i18nStore';

const i18n = createI18nStore(config);
```
