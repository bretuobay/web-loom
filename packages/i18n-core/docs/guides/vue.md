# Vue Integration Guide

## Setup

Import and use the `useI18n` composable from `examples/vue/useI18n.ts`.

## Example

```typescript
import { useI18n } from '../../examples/vue/useI18n';

const { locale, translations, t } = useI18n(config);
```
