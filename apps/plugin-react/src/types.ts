import { ComponentType } from 'react';

/**
 * Defines the shape of a React component that can be used as a plugin.
 * It's a standard React component type.
 */
export type ReactPluginComponent = ComponentType<any>;
